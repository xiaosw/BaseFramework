<?xml version="1.0" encoding="utf-8"?>

<resources>

    <!--Theme.Dialog||Theme.Holo.Light.Dialog-->
    <style name="DateWheelDialog" parent="@android:style/Theme.Holo.Light.Dialog">
        <item name="android:windowFrame">@null</item>
        <item name="android:windowIsFloating">true</item>
        <item name="android:windowIsTranslucent">false</item>
        <item name="android:background">#00000000</item>
        <item name="android:windowNoTitle">true</item>
        <item name="android:backgroundDimEnabled">true</item>
        <item name="android:windowContentOverlay">@null</item>
        <item name="android:windowBackground">@android:color/transparent</item>
    </style>

    <style name="DateWheelAnim" parent="android:Animation">
        <item name="@android:windowEnterAnimation">@anim/date_wheel_enter</item>
        //进入时的动画
        <item name="@android:windowExitAnimation">@anim/date_wheel_exit</item>
        //退出时的动画
    </style>
</resources>